const Btn = (props) => {
    const HandlleClick = (name) => {
        alert("Hello-" + name);

    };
    return(
        <button
        className="{props.classNer}">
        

        </button>
    )
}